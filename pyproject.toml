[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
# Default: sequential.  Use `pytest -n auto` for parallel execution.
# With -n auto, pytest-xdist spawns one worker per CPU core.
# Each worker builds its own BIOS/KDOS snapshot independently.
addopts = "--tb=short -q"
